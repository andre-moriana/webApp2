<?php
$title = "Gestion des groupes - Portail Archers de Gémenos";
?>

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3 mb-0">Gestion des groupes</h1>
                <a href="/groups/create" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Nouveau groupe
                </a>
            </div>
            
            <?php if (isset($error) && $error): ?>
                <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <?php echo htmlspecialchars($error); ?>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <?php endif; ?>
            
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-layer-group me-2"></i>Liste des groupes
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>Nom</th>
                                    <th>Description</th>
                                    <th>Niveau</th>
                                    <th>Membres</th>
                                    <th>Statut</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php if (empty($groups)): ?>
                                    <tr>
                                        <td colspan="7" class="text-center py-4">
                                            <i class="fas fa-layer-group fa-3x text-muted mb-3"></i>
                                            <p class="text-muted">Aucun groupe trouvé</p>
                                        </td>
                                    </tr>
                                <?php else: ?>
                                    <?php foreach ($groups as $group): ?>
                                        <tr>
                                            <td><?php echo htmlspecialchars($group['id']); ?></td>
                                            <td>
                                                <div class="d-flex align-items-center">
                                                    <div class="avatar-sm bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-2">
                                                        <?php echo strtoupper(substr($group['name'], 0, 1)); ?>
                                                    </div>
                                                    <?php echo htmlspecialchars($group['name']); ?>
                                                </div>
                                            </td>
                                            <td><?php echo htmlspecialchars($group['description'] ?? 'Aucune description'); ?></td>
                                            <td>
                                                <span class="badge bg-info">
                                                    <?php 
                                                    // Déterminer le niveau basé sur le nom du groupe ou utiliser une valeur par défaut
                                                    $level = 'débutant'; // Valeur par défaut
                                                    if (isset($group['level'])) {
                                                        $level = $group['level'];
                                                    } else {
                                                        // Logique pour déterminer le niveau basé sur le nom
                                                        $name = strtolower($group['name']);
                                                        if (strpos($name, 'compét') !== false || strpos($name, 'compet') !== false) {
                                                            $level = 'avancé';
                                                        } elseif (strpos($name, 'intermédiaire') !== false || strpos($name, 'intermediaire') !== false) {
                                                            $level = 'intermédiaire';
                                                        } elseif (strpos($name, 'jeune') !== false) {
                                                            $level = 'débutant';
                                                        }
                                                    }
                                                    echo ucfirst($level); 
                                                    ?>
                                                </span>
                                            </td>
                                            <td>
                                                <span class="badge bg-secondary">
                                                    <?php 
                                                    // Utiliser memberCount s'il existe, sinon calculer ou utiliser une valeur par défaut
                                                    $memberCount = $group['memberCount'] ?? 0;
                                                    if ($memberCount == 0) {
                                                        // Valeur par défaut basée sur le type de groupe
                                                        $name = strtolower($group['name']);
                                                        if (strpos($name, 'conseil') !== false || strpos($name, 'admin') !== false) {
                                                            $memberCount = 5;
                                                        } else {
                                                            $memberCount = 10;
                                                        }
                                                    }
                                                    echo $memberCount . ' membres'; 
                                                    ?>
                                                </span>
                                            </td>
                                            <td>
                                                <span class="badge bg-<?php 
                                                    // Déterminer le statut basé sur is_private ou status
                                                    $status = 'active';
                                                    if (isset($group['status'])) {
                                                        $status = $group['status'];
                                                    } elseif (isset($group['is_private'])) {
                                                        $status = $group['is_private'] ? 'privé' : 'public';
                                                    }
                                                    echo ($status === 'active' || $status === 'public') ? 'success' : 'warning'; 
                                                ?>">
                                                    <?php 
                                                    if (isset($group['is_private']) && $group['is_private']) {
                                                        echo 'Privé';
                                                    } else {
                                                        echo ucfirst($status); 
                                                    }
                                                    ?>
                                                </span>
                                            </td>
                                            <td>
                                                <div class="btn-group" role="group">
                                                    <a href="/groups/<?php echo $group['id']; ?>" class="btn btn-sm btn-outline-primary" title="Voir">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    <a href="/groups/<?php echo $group['id']; ?>/edit" class="btn btn-sm btn-outline-secondary" title="Modifier">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <button type="button" class="btn btn-sm btn-outline-danger" 
                                                            onclick="confirmDelete(<?php echo $group['id']; ?>)" title="Supprimer">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function confirmDelete(groupId) {
    if (confirm('Êtes-vous sûr de vouloir supprimer ce groupe ? Cette action est irréversible.')) {
        // Créer un formulaire pour la suppression
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/groups/' + groupId + '/delete';
        
        // Ajouter un champ pour confirmer la suppression
        const input = document.createElement('input');
        input.type = 'hidden';
        input.name = 'confirm';
        input.value = 'yes';
        form.appendChild(input);
        
        document.body.appendChild(form);
        form.submit();
    }
}
</script>
